# Core FastAPI dependencies
fastapi==0.109.0              # Modern web framework for building APIs
uvicorn[standard]==0.27.0     # ASGI server to run FastAPI
python-multipart==0.0.6       # Required for file uploads

# Environment and configuration
python-dotenv==1.0.0          # Load environment variables from .env file
pydantic==2.10.5              # Data validation using Python type hints (updated for pre-built wheels)
pydantic-settings==2.7.0      # Settings management

# LangChain ecosystem
langchain==0.1.0              # LangChain core library for RAG
langchain-google-genai==1.0.1        # Google Generative AI integration for embeddings and LLM
langchain-qdrant==0.1.0              # Qdrant vector store integration
langchain-community==0.0.38
langsmith==0.1.0
langchain-text-splitters==0.0.1

# langchain==0.1.13 supports langsmith>=0.1.0
# langchain-community==0.0.38 also supports langsmith>=0.1.0

# Vector database
qdrant-client>=1.9.0          # Qdrant vector database client (compatible version)

# PDF processing
PyPDF2==3.0.1                 # PDF parsing and text extraction

# Google Generative AI
google-generativeai           # Google Generative AI Python SDK

# CORS and security
python-jose[cryptography]==3.3.0  # JWT tokens (for future authentication)


# run backend at
# uvicorn main:app --reload --host 0.0.0.0 --port 8000